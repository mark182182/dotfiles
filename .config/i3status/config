general {
        output_format = "i3bar"
        colors = true
        interval = 5
}

order += "tztime local"
order += "tztime berlin"

tztime local {
        format = "%Y-%m-%d %H:%M:%S"
}

tztime budapest {
        format = "%Y-%m-%d %H:%M:%S %Z"
        timezone = "Europe/Budapest"
}
